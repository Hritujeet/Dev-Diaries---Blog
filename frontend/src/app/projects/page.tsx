import React from 'react';
import ProjectsContainer from "@/components/ProjectsContainer";

const Page = () => {
    return (
        <div>
            <ProjectsContainer />
        </div>
    );
};

export default Page;